<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.familianascimento.rodrigo.contasdacasa" >

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".ContasDaCasaApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustResize|stateVisible" >
        </activity>
        <activity
            android:name=".DespesaListActivity"
            android:exported="true"
            android:label="@string/title_despesa_list" >
        </activity>
        <activity
            android:name=".DespesaDetailActivity"
            android:label="@string/title_despesa_detail"
            android:parentActivityName=".DespesaListActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="org.familianascimento.rodrigo.contasdacasa.DespesaListActivity" />
        </activity>
        <activity
            android:name=".NovaDespesa"
            android:label="@string/title_activity_nova_despesa"
            android:parentActivityName=".DespesaListActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="org.familianascimento.rodrigo.contasdacasa.DespesaListActivity" />
        </activity>
    </application>

</manifest>
